import type { Mongodbv1 } from '@scaleway/sdk-mongodb'
import { useMongodbv1API } from '@scaleway/sdk-react/mongodbv1API'
import type { UseDataLoaderConfig } from '@scaleway/use-dataloader'
import { useDataLoader } from '@scaleway/use-dataloader'

// This file is generated by @scaleway/generate-react-queries placed in https://github.com/scaleway/scaleway-sdk-js` all modifications will be erased
export const useMongodbv1APIListInstancesAllQuery = (
  params: Mongodbv1.ListInstancesRequest,
  dataloaderConfig?: UseDataLoaderConfig<Mongodbv1.Instance[], Error>,
): ReturnType<typeof useDataLoader<Mongodbv1.Instance[], Error>> => {
  const { mongodbv1 } = useMongodbv1API()
  const key = [
    'mongodbv1',
    'listInstances',
    'all',
    ...Object.entries(params).flat(3).sort(),
  ]

  return useDataLoader(
    key,
    () => mongodbv1.listInstances(params).all(),
    dataloaderConfig,
  )
}
