import type { Blockv1 } from '@scaleway/sdk-block'
import { useBlockv1API } from '@scaleway/sdk-react/blockv1API'
import type { UseInfiniteDataLoaderConfig } from '@scaleway/use-dataloader'
import { useInfiniteDataLoader } from '@scaleway/use-dataloader'

// This file is generated by @scaleway/generate-react-queries placed in https://github.com/scaleway/scaleway-sdk-js` all modifications will be erased
export const useBlockv1APIListVolumesInfiniteQuery = (
  params: Blockv1.ListVolumesRequest,
  pageParamKey: keyof typeof params,
  dataloaderConfig?: UseInfiniteDataLoaderConfig<
    Blockv1.ListVolumesResponse,
    Error,
    typeof params,
    typeof pageParamKey
  >,
): ReturnType<
  typeof useInfiniteDataLoader<
    Blockv1.ListVolumesResponse,
    Error,
    typeof params,
    typeof pageParamKey
  >
> => {
  const { blockv1 } = useBlockv1API()
  const key = [
    'blockv1',
    'listVolumes',
    ...Object.entries(params).flat(3).sort(),
  ]

  return useInfiniteDataLoader(
    key,
    requestParams => blockv1.listVolumes(requestParams),
    params,
    pageParamKey,
    dataloaderConfig,
  )
}
