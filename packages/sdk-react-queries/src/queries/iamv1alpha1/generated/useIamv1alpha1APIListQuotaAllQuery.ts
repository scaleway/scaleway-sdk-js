import type { Iamv1alpha1 } from '@scaleway/sdk-iam'
import { useIamv1alpha1API } from '@scaleway/sdk-react/iamv1alpha1API'
import type { UseDataLoaderConfig } from '@scaleway/use-dataloader'
import { useDataLoader } from '@scaleway/use-dataloader'

// This file is generated by @scaleway/generate-react-queries placed in https://github.com/scaleway/scaleway-sdk-js` all modifications will be erased
export const useIamv1alpha1APIListQuotaAllQuery = (
  params: Iamv1alpha1.ListQuotaRequest,
  dataloaderConfig?: UseDataLoaderConfig<Iamv1alpha1.Quotum[], Error>,
): ReturnType<typeof useDataLoader<Iamv1alpha1.Quotum[], Error>> => {
  const { iamv1alpha1 } = useIamv1alpha1API()
  const key = [
    'iamv1alpha1',
    'listQuota',
    'all',
    ...Object.entries(params).flat(3).sort(),
  ]

  return useDataLoader(
    key,
    () => iamv1alpha1.listQuota(params).all(),
    dataloaderConfig,
  )
}
