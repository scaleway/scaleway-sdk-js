import { useDataLoaderContext } from "@scaleway/use-dataloader"

// This file is generated by @scaleway/generate-react-queries placed in https://github.com/scaleway/scaleway-sdk-js` all modifications will be erased
export const useBlockv1APIReload = (): { reloadBlockv1API: () => void; asyncReloadBlockv1API: () => Promise<void> } => {
  const dataLoader = useDataLoaderContext()
  const keyPrefix = ["blockv1"]

  return {
    reloadBlockv1API: () => dataLoader.reloadGroupActive(keyPrefix).catch(() => null),
    asyncReloadBlockv1API: () => dataLoader.reloadGroupActive(keyPrefix),
  }
}
