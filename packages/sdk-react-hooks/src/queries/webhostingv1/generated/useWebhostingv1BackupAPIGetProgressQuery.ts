import { useWebhostingv1BackupAPI } from '@scaleway/sdk-react/webhostingv1BackupAPI'
import type { Webhostingv1 } from '@scaleway/sdk-webhosting'
import type { UseDataLoaderConfig } from '@scaleway/use-dataloader'
import { useDataLoader } from '@scaleway/use-dataloader'

// This file is generated by @scaleway/generate-react-queries placed in https://github.com/scaleway/scaleway-sdk-js` all modifications will be erased
export const useWebhostingv1BackupAPIGetProgressQuery = (
  params: Webhostingv1.BackupApiGetProgressRequest,
  dataloaderConfig?: UseDataLoaderConfig<Webhostingv1.Progress, Error>,
): ReturnType<typeof useDataLoader<Webhostingv1.Progress, Error>> => {
  const { webhostingv1Backup } = useWebhostingv1BackupAPI()
  const key = [
    'webhostingv1Backup',
    'getProgress',
    ...Object.entries(params).flat(3).sort(),
  ]

  return useDataLoader(
    key,
    () => webhostingv1Backup.getProgress(params),
    dataloaderConfig,
  )
}
