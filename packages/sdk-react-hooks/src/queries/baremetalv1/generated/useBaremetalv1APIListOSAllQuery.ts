import type { Baremetalv1 } from '@scaleway/sdk-baremetal'
import { useBaremetalv1API } from '@scaleway/sdk-react/baremetalv1API'
import type { UseDataLoaderConfig } from '@scaleway/use-dataloader'
import { useDataLoader } from '@scaleway/use-dataloader'

// This file is generated by @scaleway/generate-react-queries placed in https://github.com/scaleway/scaleway-sdk-js` all modifications will be erased
export const useBaremetalv1APIListOSAllQuery = (
  params: Baremetalv1.ListOSRequest,
  dataloaderConfig?: UseDataLoaderConfig<Baremetalv1.OS[], Error>,
): ReturnType<typeof useDataLoader<Baremetalv1.OS[], Error>> => {
  const { baremetalv1 } = useBaremetalv1API()
  const key = [
    'baremetalv1',
    'listOS',
    'all',
    ...Object.entries(params).flat(3).sort(),
  ]

  return useDataLoader(
    key,
    () => baremetalv1.listOS(params).all(),
    dataloaderConfig,
  )
}
