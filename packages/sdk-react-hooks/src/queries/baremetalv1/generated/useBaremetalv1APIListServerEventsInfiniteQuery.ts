import type { Baremetalv1 } from '@scaleway/sdk-baremetal'
import { useBaremetalv1API } from '@scaleway/sdk-react/baremetalv1API'
import type { UseInfiniteDataLoaderConfig } from '@scaleway/use-dataloader'
import { useInfiniteDataLoader } from '@scaleway/use-dataloader'

// This file is generated by @scaleway/generate-react-queries placed in https://github.com/scaleway/scaleway-sdk-js` all modifications will be erased
export const useBaremetalv1APIListServerEventsInfiniteQuery = (
  params: Baremetalv1.ListServerEventsRequest,
  pageParamKey: keyof typeof params,
  dataloaderConfig?: UseInfiniteDataLoaderConfig<
    Baremetalv1.ListServerEventsResponse,
    Error,
    typeof params,
    typeof pageParamKey
  >,
): ReturnType<
  typeof useInfiniteDataLoader<
    Baremetalv1.ListServerEventsResponse,
    Error,
    typeof params,
    typeof pageParamKey
  >
> => {
  const { baremetalv1 } = useBaremetalv1API()
  const key = [
    'baremetalv1',
    'listServerEvents',
    ...Object.entries(params).flat(3).sort(),
  ]

  return useInfiniteDataLoader(
    key,
    requestParams => baremetalv1.listServerEvents(requestParams),
    params,
    pageParamKey,
    dataloaderConfig,
  )
}
