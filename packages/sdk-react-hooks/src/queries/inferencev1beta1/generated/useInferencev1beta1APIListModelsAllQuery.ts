import type { Inferencev1beta1 } from '@scaleway/sdk-inference'
import { useInferencev1beta1API } from '@scaleway/sdk-react/inferencev1beta1API'
import type { UseDataLoaderConfig } from '@scaleway/use-dataloader'
import { useDataLoader } from '@scaleway/use-dataloader'

// This file is generated by @scaleway/generate-react-queries placed in https://github.com/scaleway/scaleway-sdk-js` all modifications will be erased
export const useInferencev1beta1APIListModelsAllQuery = (
  params: Inferencev1beta1.ListModelsRequest,
  dataloaderConfig?: UseDataLoaderConfig<Inferencev1beta1.Model[], Error>,
): ReturnType<typeof useDataLoader<Inferencev1beta1.Model[], Error>> => {
  const { inferencev1beta1 } = useInferencev1beta1API()
  const key = [
    'inferencev1beta1',
    'listModels',
    'all',
    ...Object.entries(params).flat(3).sort(),
  ]

  return useDataLoader(
    key,
    () => inferencev1beta1.listModels(params).all(),
    dataloaderConfig,
  )
}
