import { useDataLoaderContext } from "@scaleway/use-dataloader"

// This file is generated by @scaleway/generate-react-queries placed in https://github.com/scaleway/scaleway-sdk-js` all modifications will be erased
export const useBillingv2APIReload = (): { reloadBillingv2API: () => void; asyncReloadBillingv2API: () => Promise<void> } => {
  const dataLoader = useDataLoaderContext()
  const keyPrefix = ["billingv2"]

  return {
    reloadBillingv2API: () => dataLoader.reloadGroupActive(keyPrefix).catch(() => null),
    asyncReloadBillingv2API: () => dataLoader.reloadGroupActive(keyPrefix),
  }
}
