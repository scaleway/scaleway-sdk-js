import { useDataLoaderContext } from "@scaleway/use-dataloader"

// This file is generated by @scaleway/generate-react-queries placed in https://github.com/scaleway/scaleway-sdk-js` all modifications will be erased
export const useIpamv1APIReload = (): { reloadIpamv1API: () => void; asyncReloadIpamv1API: () => Promise<void> } => {
  const dataLoader = useDataLoaderContext()
  const keyPrefix = ["ipamv1"]

  return {
    reloadIpamv1API: () => dataLoader.reloadGroupActive(keyPrefix).catch(() => null),
    asyncReloadIpamv1API: () => dataLoader.reloadGroupActive(keyPrefix),
  }
}
