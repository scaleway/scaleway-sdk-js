import type { Instancev1 } from '@scaleway/sdk-instance'
import { useInstancev1API } from '@scaleway/sdk-react/instancev1API'
import type { UseDataLoaderConfig } from '@scaleway/use-dataloader'
import { useDataLoader } from '@scaleway/use-dataloader'

// This file is generated by @scaleway/generate-react-queries placed in https://github.com/scaleway/scaleway-sdk-js` all modifications will be erased
export const useInstancev1APIListImagesAllQuery = (
  params: Instancev1.ListImagesRequest,
  dataloaderConfig?: UseDataLoaderConfig<Instancev1.Image[], Error>,
): ReturnType<typeof useDataLoader<Instancev1.Image[], Error>> => {
  const { instancev1 } = useInstancev1API()
  const key = [
    'instancev1',
    'listImages',
    'all',
    ...Object.entries(params).flat(3).sort(),
  ]

  return useDataLoader(
    key,
    () => instancev1.listImages(params).all(),
    dataloaderConfig,
  )
}
