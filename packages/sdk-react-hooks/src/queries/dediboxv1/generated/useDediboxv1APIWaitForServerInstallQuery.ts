import type { Dediboxv1 } from '@scaleway/sdk-dedibox'
import { useDediboxv1API } from '@scaleway/sdk-react/dediboxv1API'
import type { UseDataLoaderConfig } from '@scaleway/use-dataloader'
import { useDataLoader } from '@scaleway/use-dataloader'

// This file is generated by @scaleway/generate-react-queries placed in https://github.com/scaleway/scaleway-sdk-js` all modifications will be erased
export const useDediboxv1APIWaitForServerInstallQuery = (
  params: Dediboxv1.GetServerInstallRequest,
  dataloaderConfig?: UseDataLoaderConfig<Dediboxv1.ServerInstall, Error>,
): ReturnType<typeof useDataLoader<Dediboxv1.ServerInstall, Error>> => {
  const { dediboxv1 } = useDediboxv1API()
  const key = [
    'dediboxv1',
    'waitForServerInstall',
    ...Object.entries(params).flat(3).sort(),
  ]

  return useDataLoader(
    key,
    () => dediboxv1.waitForServerInstall(params),
    dataloaderConfig,
  )
}
