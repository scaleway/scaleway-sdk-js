import { useDataLoaderContext } from '@scaleway/use-dataloader'

// This file is generated by @scaleway/generate-react-queries placed in https://github.com/scaleway/scaleway-sdk-js` all modifications will be erased
export const useDediboxv1APIReload = (): {
  reloadDediboxv1API: () => void
  asyncReloadDediboxv1API: () => Promise<void>
} => {
  const dataLoader = useDataLoaderContext()
  const keyPrefix = ['dediboxv1']

  return {
    reloadDediboxv1API: () =>
      dataLoader.reloadGroupActive(keyPrefix).catch(() => null),
    asyncReloadDediboxv1API: () => dataLoader.reloadGroupActive(keyPrefix),
  }
}
