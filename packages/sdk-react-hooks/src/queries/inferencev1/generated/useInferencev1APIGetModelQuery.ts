import { useInferencev1API } from "@scaleway/sdk-react/inferencev1API"
import type { Inferencev1 } from "@scaleway/sdk-inference"
import type { UseDataLoaderConfig } from "@scaleway/use-dataloader"
import { useDataLoader } from "@scaleway/use-dataloader"

// This file is generated by @scaleway/generate-react-queries placed in https://github.com/scaleway/scaleway-sdk-js` all modifications will be erased
export const useInferencev1APIGetModelQuery = (
  params: Inferencev1.GetModelRequest,
  dataloaderConfig?: UseDataLoaderConfig<Inferencev1.Model, Error>,
): ReturnType<typeof useDataLoader<Inferencev1.Model, Error>> => {
  const { inferencev1 } = useInferencev1API()
  const key = ["inferencev1", "getModel", ...Object.entries(params).flat(3).sort()]

  return useDataLoader(key, () => inferencev1.getModel(params), dataloaderConfig)
}
