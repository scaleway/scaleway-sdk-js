import { useMarketplacev2API } from "@scaleway/sdk-react/marketplacev2API"
import type { Marketplacev2 } from "@scaleway/sdk-marketplace"
import type { UseDataLoaderConfig } from "@scaleway/use-dataloader"
import { useDataLoader } from "@scaleway/use-dataloader"

// This file is generated by @scaleway/generate-react-queries placed in https://github.com/scaleway/scaleway-sdk-js` all modifications will be erased
export const useMarketplacev2APIListLocalImagesAllQuery = (
  params: Marketplacev2.ListLocalImagesRequest,
  dataloaderConfig?: UseDataLoaderConfig<Marketplacev2.LocalImage[], Error>,
): ReturnType<typeof useDataLoader<Marketplacev2.LocalImage[], Error>> => {
  const { marketplacev2 } = useMarketplacev2API()
  const key = ["marketplacev2", "listLocalImages", "all", ...Object.entries(params).flat(3).sort()]

  return useDataLoader(key, () => marketplacev2.listLocalImages(params).all(), dataloaderConfig)
}
