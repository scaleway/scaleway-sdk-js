{
  "name": "@repo/npm-trust",
  "version": "1.0.0",
  "private": true,
  "description": "Configure npm trusted publishers (GitHub Actions) for workspace packages",
  "repository": {
    "type": "git",
    "url": "https://github.com/scaleway/scaleway-sdk-js.git",
    "directory": "tools/npm-trust"
  },
  "bin": {
    "npm-trust": "./src/cli.ts"
  },
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@repo/configs": "workspace:*",
    "@types/node": "catalog:"
  },
  "engines": {
    "node": ">=22.x",
    "pnpm": ">=12.x"
  }
}
